# 翻译问题修复指南

共发现 7 个问题需要修复。

## fr

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `fr/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## hi

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `hi/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## mk

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `mk/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## pt

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `pt/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## sw

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `sw/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## uz

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `uz/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

## vi

### tree-editor.ts

#### 缺少的键

- `addItem`
- `addProperty`
- `keyName`
- `value`
- `add`
- `cancel`
- `save`
- `empty`
- `keyNameEmpty`
- `addFailed`
- `invalidValue`

#### 修复建议

1. 在 `vi/tree-editor.ts` 文件中添加以下缺少的键 (从英文版本复制并翻译):

   - `addItem`
   - `addProperty`
   - `keyName`
   - `value`
   - `add`
   - `cancel`
   - `save`
   - `empty`
   - `keyNameEmpty`
   - `addFailed`
   - `invalidValue`

